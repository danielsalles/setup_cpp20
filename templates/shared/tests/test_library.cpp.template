#include <catch2/catch_test_macros.hpp>
#include <library.hpp>

TEST_CASE("{{ project_name }} library functionality", "[library]") {
    SECTION("Library can be instantiated") {
        // TODO: Add basic library instantiation tests here
        // Example:
        // auto instance = {{ project_name | lower }}::create_instance();
        // REQUIRE(instance != nullptr);
        
        // For now, just verify compilation works
        SUCCEED("Library compiles and links correctly");
    }
    
    SECTION("Library version is accessible") {
        // TODO: If your library provides version info, test it here
        // Example:
        // auto version = {{ project_name | lower }}::get_version();
        // REQUIRE(version.major >= 1);
        
        SUCCEED("Library version test placeholder");
    }
}

// TODO: Add specific tests for your library's public API
// Replace these with actual tests based on your library's functionality 